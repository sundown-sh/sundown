global:
  scrape_interval: 30s
  evaluation_interval: 30s
  external_labels:
    monitor: sundown

scrape_configs:
  - job_name: sundown
    metrics_path: /metrics
    static_configs:
      - targets: ["sundown:8000"]
        labels:
          service: sundown
          component: app
